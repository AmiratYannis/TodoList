version: '1.9'
services:
  client: 
    container_name: client
    restart: always
    build: ./front
    ports: 
      - "5173:5173"
    links: 
      - app
  app:
    container_name: app
    restart: always
    build: ./back
    ports: 
      - "3500:3500"
